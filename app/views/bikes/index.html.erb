<h1>All Bikes</h1>

<div style='width: 800px;'>
  <div id="map" style='width: 800px; height: 400px;'></div>
</div>

<% @bikes.each do |bike| %>
	<ul>
		<li><%= bike.brand %></li>
		<li><%= bike.category %></li>
		<li><%= bike.description %></li>
		<li><%= bike.year_made %></li>
		<li>
			<%= link_to 'Edit', edit_bike_path(bike) %> | 
			<%= link_to 'Destroy', bike_path(bike),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
		</li>
	</ul>

	<% end %>
